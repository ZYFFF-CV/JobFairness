# Install the shared FuxiCTR dependencies before the FairJob-specific pins.
-r ../../requirements.txt

# FuxiCTR 2.3.9 passes eps to sklearn.metrics.log_loss. Scikit-learn 1.5+
# removed that argument, so keep a Python-version-compatible release below it.
scikit-learn==1.3.2; python_version < "3.12"
scikit-learn==1.4.2; python_version >= "3.12"

# Match the locally validated FairJob XGBoost integration environment.
xgboost==2.1.4
